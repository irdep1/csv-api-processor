{
  "requests": [
    {
      "endpoint": "https://httpbin.org/post",
      
      "method": "POST",
      "payloadTemplate": {
        "productName": "$productName",
        "price": "$price",
        "description": "$description"
      }
    }
  ]
}
